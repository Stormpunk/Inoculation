using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this script will be put on the "character" of the sprite, that moves forward. It will leave behind the spread as it moves to simulate 
public class TheSpreadActor : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
